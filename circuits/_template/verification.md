# 验证记录

> 状态：🟡 **待验证**（与 README front-matter 的 `status` 保持一致）
> 验证人：________　日期：________　Multisim 版本：________（目标 14.3）

## 1. 验证环境

| 项 | 值 |
|----|----|
| Multisim 版本 | 例：14.3.0（教育版） |
| 操作系统 | 例：Windows 11 23H2 |
| 导入方式 | `File → Open` → 文件类型选 `SPICE netlist (*.cir)` |
| 是否黑盒替换 | 是 / 否，替换了哪些： |
| 分析是否重设 | 是 / 否，在 `Simulate → Analyses and simulation` 里设了： |

## 2. 理论值 vs 实测值

> **实测值必须来自真实 Grapher / 仪器读数**，不允许抄理论值，不允许写"≈"。
> 测不出来就留空并把状态保持 `待验证`。

| # | 观测量 | 理论值 | 实测值 | 误差 | 结论 |
|---|--------|--------|--------|------|------|
| 1 | | | | | ✅ / ❌ |
| 2 | | | | | |
| 3 | | | | | |
| 4 | | | | | |
| 5 | | | | | |

## 3. 手算过程（用网表里的元件值，逐步列式）

```text
（把推导过程写在这里，方便 reviewer 复核第 2 节的理论值）
```

## 4. 导入过程记录

| 步骤 | 现象 | 是否正常 |
|------|------|---------|
| File → Open 导入 .cir | 例：报 3 个 warning，全部元件识别 | |
| 元件摆放 | 例：按行序左→右排布，需手动旋转 3 个 | |
| 连线 | 例：无悬空节点 | |
| 首次 Run | 例：报 "singular matrix"，加了 Rxxx 后正常 | |

## 5. 截图

> 建议放：① Multisim 原理图 ② Grapher 波形（带游标） ③ 仪器面板设置。
> 放在本目录下 `assets/` 子目录，用相对路径引用。

（暂无）

## 6. 结论

- [ ] 全部观测量在可接受误差内 → 状态改为 🟢 `已验证`，同步改 README front-matter
- [ ] 有观测量超差但已定位原因 → 状态保持 🔴 `验证失败`，在下面写清原因
- [ ] 尚未验证 → 状态保持 🟡 `待验证`

**备注 / 已知问题**：
